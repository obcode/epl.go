# Build output. Das Binary muss `epl` heißen, nicht `epl.go` — sonst kompiliert der nächste
# Build das eigene Binary als Quelldatei mit. Erzwungen in `gowatch.yml`.
/epl
/dist/

# Konfiguration mit Secrets. Nur lokal bzw. auf dem Host.
epl.yaml
.env

# Claude
.claude/settings.local.json

# macOS
.DS_Store
