# Build output. Das Binary muss `tallox` heißen, nicht `tallox.go` — sonst kompiliert
# der nächste Build das eigene Binary als Quelldatei mit. Erzwungen in `gowatch.yml`.
/tallox
/dist/

# Konfiguration mit Secrets. Nur lokal bzw. auf dem Host.
tallox.yaml
.env

# Claude
.claude/settings.local.json

# macOS
.DS_Store
