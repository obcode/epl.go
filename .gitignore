# Build output. Das Binary heißt wie ein Verzeichnis wäre — nach `go build -o epl .` bricht
# gowatch, deshalb im Zweifel `go run .` benutzen.
/epl
/dist/

# Konfiguration mit Secrets. Nur lokal bzw. auf dem Host.
epl.yaml
.env

# Claude
.claude/settings.local.json

# macOS
.DS_Store
