// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.0
// source: token.sql

package store

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createToken = `-- name: CreateToken :one

INSERT INTO personal_access_token (
    token_id, owner_id, secret_hash, description, scopes, expires_at
)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING token_id, owner_id, description, scopes, created_at, expires_at,
          last_used_at, revoked_at
`

type CreateTokenParams struct {
	TokenID     string
	OwnerID     uuid.UUID
	SecretHash  []byte
	Description string
	Scopes      []string
	ExpiresAt   time.Time
}

type CreateTokenRow struct {
	TokenID     string
	OwnerID     uuid.UUID
	Description string
	Scopes      []string
	CreatedAt   time.Time
	ExpiresAt   time.Time
	LastUsedAt  pgtype.Timestamptz
	RevokedAt   pgtype.Timestamptz
}

// Personal Access Tokens: the second door.
// The secret is generated and hashed by the caller (internal/auth), never here: a secret that
// travelled through a query is a secret in a log somewhere.
func (q *Queries) CreateToken(ctx context.Context, arg CreateTokenParams) (CreateTokenRow, error) {
	row := q.db.QueryRow(ctx, createToken,
		arg.TokenID,
		arg.OwnerID,
		arg.SecretHash,
		arg.Description,
		arg.Scopes,
		arg.ExpiresAt,
	)
	var i CreateTokenRow
	err := row.Scan(
		&i.TokenID,
		&i.OwnerID,
		&i.Description,
		&i.Scopes,
		&i.CreatedAt,
		&i.ExpiresAt,
		&i.LastUsedAt,
		&i.RevokedAt,
	)
	return i, err
}

const listTokensOfPerson = `-- name: ListTokensOfPerson :many
SELECT token_id, owner_id, description, scopes, created_at, expires_at,
       last_used_at, revoked_at
FROM personal_access_token
WHERE owner_id = $1
ORDER BY created_at DESC
`

type ListTokensOfPersonRow struct {
	TokenID     string
	OwnerID     uuid.UUID
	Description string
	Scopes      []string
	CreatedAt   time.Time
	ExpiresAt   time.Time
	LastUsedAt  pgtype.Timestamptz
	RevokedAt   pgtype.Timestamptz
}

// The token list in the GUI. The secret hash is not in the projection: nothing outside
// authentication has a use for it, and a column that is never selected cannot be logged.
func (q *Queries) ListTokensOfPerson(ctx context.Context, ownerID uuid.UUID) ([]ListTokensOfPersonRow, error) {
	rows, err := q.db.Query(ctx, listTokensOfPerson, ownerID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListTokensOfPersonRow{}
	for rows.Next() {
		var i ListTokensOfPersonRow
		if err := rows.Scan(
			&i.TokenID,
			&i.OwnerID,
			&i.Description,
			&i.Scopes,
			&i.CreatedAt,
			&i.ExpiresAt,
			&i.LastUsedAt,
			&i.RevokedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markTokenUsed = `-- name: MarkTokenUsed :exec
UPDATE personal_access_token
SET last_used_at = now()
WHERE token_id = $1
  AND (last_used_at IS NULL OR last_used_at < now() - interval '5 minutes')
`

// Coarse on purpose. "Last used" is answering "is this token still in use, can I revoke it",
// and five-minute resolution answers that as well as microsecond resolution would.
//
// The guard is the point: without it, every API call takes a row lock and writes WAL, which
// serialises the requests of a single script against itself and makes the busiest tokens the
// slowest ones.
func (q *Queries) MarkTokenUsed(ctx context.Context, tokenID string) error {
	_, err := q.db.Exec(ctx, markTokenUsed, tokenID)
	return err
}

const revokeToken = `-- name: RevokeToken :exec
UPDATE personal_access_token
SET revoked_at = now()
WHERE token_id = $1 AND revoked_at IS NULL
`

// A timestamp, not a DELETE: the audit log has to keep resolving this token id afterwards.
// Idempotent — revoking twice keeps the first moment, which is the one that matters.
func (q *Queries) RevokeToken(ctx context.Context, tokenID string) error {
	_, err := q.db.Exec(ctx, revokeToken, tokenID)
	return err
}

const tokenByID = `-- name: TokenByID :one
SELECT
    t.token_id,
    t.owner_id,
    t.secret_hash,
    t.scopes,
    t.expires_at,
    t.revoked_at,
    p.mail,
    p.name,
    p.active,
    COALESCE(
        array_agg(pr.role ORDER BY pr.role) FILTER (WHERE pr.role IS NOT NULL),
        ARRAY[]::text[]
    )::text[] AS roles
FROM personal_access_token t
JOIN person p ON p.id = t.owner_id
LEFT JOIN person_role pr ON pr.person_id = p.id
WHERE t.token_id = $1
GROUP BY t.token_id, p.id
`

type TokenByIDRow struct {
	TokenID    string
	OwnerID    uuid.UUID
	SecretHash []byte
	Scopes     []string
	ExpiresAt  time.Time
	RevokedAt  pgtype.Timestamptz
	Mail       string
	Name       string
	Active     bool
	Roles      []string
}

// The authentication query of the token door, in one round trip: the token, its owner and the
// owner's roles.
//
// Permissions come from the person on every request, which is what makes "a token can never
// exceed its owner's role" true by construction — revoking a role demotes every token that
// person holds without touching a token row.
//
// Deliberately returns expired, revoked and inactive rows instead of filtering them out.
// Authentication has to tell "this token does not exist" from "this token expired" — the
// caller owns the token and can be told why it stopped working — and a query that filters
// would collapse both into the same silence. It also keeps the timing of the two cases
// similar, which a WHERE clause on revoked_at would not.
func (q *Queries) TokenByID(ctx context.Context, tokenID string) (TokenByIDRow, error) {
	row := q.db.QueryRow(ctx, tokenByID, tokenID)
	var i TokenByIDRow
	err := row.Scan(
		&i.TokenID,
		&i.OwnerID,
		&i.SecretHash,
		&i.Scopes,
		&i.ExpiresAt,
		&i.RevokedAt,
		&i.Mail,
		&i.Name,
		&i.Active,
		&i.Roles,
	)
	return i, err
}
